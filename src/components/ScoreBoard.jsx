// ScoreBoard is now rendered inline in Game.jsx as badges
// This component is kept for compatibility but not used directly
function ScoreBoard({ score, bestScore }) {
  return null;
}
export default ScoreBoard;
